# --- Gradle / 빌드 산출물 -------------------------------------------------
.gradle/
build/
app/build/
captures/

# --- 로컬 환경 (절대 올리면 안 됨) ---------------------------------------
local.properties

# --- IDE ------------------------------------------------------------------
.idea/
*.iml
.DS_Store

# --- 서명 키 (절대 올리면 안 됨) ------------------------------------------
*.keystore
*.jks
*.p12
*.pem
keystore.properties
signing.properties
key.properties
google-services.json

# --- 로컬 도구 설정 -------------------------------------------------------
.claude/settings.local.json

# --- 작업 백업 (소스셋 밖에 보관해야 컴파일에 섞이지 않음) ----------------
_backup/

# --- 기타 -----------------------------------------------------------------
*.apk
*.aab
*.log
.cxx/
.externalNativeBuild/
dist/
